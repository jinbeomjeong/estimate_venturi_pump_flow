# 유량 추정 모델 (v2)

`utils.assemble_model.build_flow_model_v2`

## 한 줄 요약

압력 2채널(흡입·토출)의 20스텝 창을 여러 길이로 평균 낸 뒤, **선형 줄기**로 유량을
예측하고 그 위에 **작은 비선형 보정**을 얹습니다. 파라미터는 1,455개입니다.

```
입력 (20, 3)  ← 로깅/추론 버퍼는 그대로 3채널
   │
   ├ ChannelSelect(0, 1)          펌프 회전수를 구조적으로 제외
   ├ Rescaling(고정 상수)          학습 데이터의 평균/표준편차, 샘플별 정규화 금지
   ├ ChannelGate                   채널별 학습 스칼라 + L2 벌점
   ├ MultiScaleSmoothing(1,3,5,10,20)   뒤쪽 1·3·5·10·20스텝 평균 → 10차원
   │
   ├──────────────── linear_trunk : Dense(1, linear)          ← 예측의 주축
   │
   └ Dense(32) → Dense(32) → correction : Dense(1, linear)    ← 보정
                    │
              ScaledResidual : trunk + alpha * correction  (alpha는 0에서 시작)
                    │
              Rescaling(target_std, target_mean) → 유량(LPM)
```

## 왜 이 구조인가

이전 모델은 팽창 인과 합성곱 5블록(약 50만 파라미터)이었습니다. 구조를 바꾼
근거는 전부 이 저장소의 로그 데이터에서 측정한 것입니다.

### 1. 창의 역할은 '동특성'이 아니라 '잡음 제거'였다

압력 2채널만으로 선형 회귀를 돌렸을 때:

| 입력 | 검증 MAPE |
|---|---|
| 마지막 1샘플 | 4.75% |
| 뒤쪽 5샘플 평균 | 4.52% |
| 20샘플 전체 평균 | 4.34% |

단순 평균만으로 좋아집니다. 즉 20스텝 창이 담고 있는 정보는 대부분 "같은 값을
여러 번 잰 것"입니다. 그래서 평균을 팽창 합성곱이 스스로 발견하도록 두지 않고
`MultiScaleSmoothing`이 직접 만들어 넘깁니다.

### 2. 선형 모델이 이미 기존 심층망만큼 정확했다

20샘플 평균 압력에 대한 **3-파라미터 선형 회귀**가 기존 50만 파라미터 모델과
사실상 같은 검증 성능을 냈습니다. 용량이 정확도로 바뀌지 않고 학습 세션을
외우는 데 쓰이고 있었다는 뜻입니다. 그래서 선형 줄기를 주축으로 두고,
비선형은 `ScaledResidual`의 `alpha`가 자라는 만큼만 개입하게 했습니다.
`alpha`는 학습 후 값을 찍어보면 비선형 항이 실제로 얼마나 기여하는지 바로 보입니다.

### 3. 진짜 오차원은 '세션 간 이동'이다

세션별로 따로 선형 회귀를 맞추면:

| 세션 | 흡입압 계수 | 토출압 계수 | 자체 R² |
|---|---|---|---|
| 학습 (train_data) | **−7,719** | 769 | 0.939 |
| 검증 (11-17) | **−244** | 825 | 0.995 |
| 테스트 (11-14) | **+1,177** | 890 | 0.913 |

한 세션 안에서는 관계가 거의 완벽한 직선입니다(검증 R² 0.995). 그런데 **흡입압
계수는 세션이 바뀌면 부호까지 뒤집힙니다.** 토출압 계수는 769~890으로 안정적입니다.

흡입압을 빼고 토출압만 쓰면 모든 지표가 좋아집니다:

| 입력 | 검증 MAPE | 테스트 MAPE |
|---|---|---|
| 흡입 + 토출 | 4.34% | 6.68% |
| **토출만** | **4.17%** | **5.98%** |

그렇다고 흡입압 센서를 코드에서 지우지는 않았습니다. 흡입측 정보(호스 길이,
흡입 양정)는 물리적으로 의미가 있고, 지금 데이터에서 변화 폭이 작아 드러나지
않는 것일 수 있기 때문입니다. 대신 `ChannelGate`가 채널마다 학습 가능한 스칼라를
두고 **L2 벌점을 매겨**, 흡입압을 쓰려면 그만한 값을 해야 하도록 만들었습니다.
초깃값은 흡입 0.3 / 토출 1.0입니다.

### 4. 절대 크기 정보를 버리지 않는다

이전 모델은 `Flatten` 직후 샘플 단위 정규화를 걸어, 마지막 단이 "패턴 모양"만
보게 되어 있었습니다. v2는 **학습 데이터 통계로 만든 고정 상수 스케일링**만
씁니다. 유량은 압력의 절대값에서 읽어야 하는 값이기 때문입니다.

타깃 평균/표준편차도 마지막 `Rescaling` 레이어에 접어 넣어, 학습은 표준화된
공간에서 하되 모델이 내보내는 값은 그대로 LPM입니다. 추론 쪽에서 후처리가
필요 없습니다.

### 5. 회전수는 쓰지 않는다

`ChannelSelect(channels=(0, 1))`가 입력에서 회전수 채널을 잘라냅니다. 입력
shape는 `(20, 3)` 그대로라 로깅 포맷과 `inference_modbus.py`의 버퍼를 바꿀
필요가 없고, 동시에 회전수가 예측에 영향을 줄 수 없음이 구조로 보장됩니다.

> 참고: 이전 배포 모델(`models/model_20.onnx`)은 회전수를 **의도치 않게**
> 무시하고 있었습니다. 크기가 1인 축에 `LayerNormalization(axis=-1)`을 걸어
> 그 채널이 상수가 되어 있었기 때문입니다. v2에서는 이것이 사고가 아니라
> 명시적인 설계 결정입니다.

## 평가 방법

세션이 다르면 관계가 이동하므로, 세 갈래로 나눠 씁니다.

| 갈래 | 데이터 | 용도 |
|---|---|---|
| 학습 | `data/train_data/*.csv` | 가중치 학습 |
| 검증 | `data/test_data/data20251117-*.csv` (5개) | 조기 종료·학습률 조정 → **낙관적** |
| 테스트 | `data/test_data/data20251114-170633.csv` | 아무 데도 쓰지 않음 → **이 숫자를 믿는다** |

테스트 파일은 기존 노트북이 한 번도 쓰지 않던 세션입니다.

데이터 처리에서 고친 점 두 가지:

* 학습 csv에는 헤더 행이 없는데 `pd.read_csv(...)`를 `header=None` 없이 부르면
  첫 샘플이 헤더로 먹혀 사라집니다 → `header=None` 지정.
* 운전 범위 밖 샘플을 지운 뒤 이어붙이면 시퀀스 창이 그 틈을 가로지릅니다
  → 연속 구간 단위로 나눈 뒤 구간 안에서만 창을 만듭니다(`split_valid_runs`).

## 사용법

```bash
python train_flow_model.py                 # 학습 + 3갈래 성능 출력 + models/model_20_v2.keras 저장
python train_flow_model.py --no-save       # 숫자만 확인
python -m utils.converte_onnx              # models/model_20_v2.onnx 로 변환
```

`inference_modbus.py`는 `models/model_20_v2.onnx`를 읽습니다. 입력 버퍼 모양은
이전과 같습니다.
