# 유량 추정 모델 (v2)

`utils.assemble_model.build_flow_model_v2`

## 한 줄 요약

압력 2채널(흡입·토출)의 10초 창을 받아, 모델 안에서 **토출압과 차압**으로 바꾼 뒤
여러 길이로 평균 내고, **선형 줄기**로 유량을 예측하고 그 위에 **작은 비선형
보정**을 얹습니다. 파라미터는 1,455개입니다.

```
입력 (20, 2)  [흡입압, 토출압]   ← 모델이 쓰는 것이 곧 입력
   │
   ├ DifferentialPressure        [토출압, 토출압 − 흡입압]  (파라미터 0)
   ├ Rescaling(고정 상수)         학습 데이터 평균/표준편차, 샘플별 정규화 금지
   ├ ChannelGate                  채널별 학습 스칼라 + L2 벌점
   ├ MultiScaleSmoothing(1,3,5,10,20)   뒤쪽 n스텝 평균 → 10차원
   │
   ├──────────────── linear_trunk : Dense(1)                  ← 예측의 주축
   │
   └ Dense(32) → Dense(32) → correction : Dense(1)            ← 곡률 보정
                    │
              ScaledResidual : trunk + alpha × correction  (alpha는 0에서 시작)
                    │
              Rescaling(target_std, target_mean) → 유량(LPM)
```

## 레이어별 입출력

| # | 레이어 | 타입 | 입력 | 출력 | 파라미터 |
|---:|---|---|---|---|---:|
| 0 | `input_layer` | InputLayer | — | `(B, 20, 2)` | 0 |
| 1 | `select_channels` | ChannelSelect | `(B, 20, 2)` | `(B, 20, 2)` | 0 |
| 2 | `differential_pressure` | DifferentialPressure | `(B, 20, 2)` | `(B, 20, 2)` | 0 |
| 3 | `input_scaling` | Rescaling | `(B, 20, 2)` | `(B, 20, 2)` | 0 |
| 4 | `channel_gate` | ChannelGate | `(B, 20, 2)` | `(B, 20, 2)` | 2 |
| 5 | `multi_scale` | MultiScaleSmoothing | `(B, 20, 2)` | `(B, 10)` | 0 |
| 6 | `dense_8` | Dense · gelu | `(B, 10)` | `(B, 32)` | 352 |
| 7 | `dropout_8` | Dropout 0.1 | `(B, 32)` | `(B, 32)` | 0 |
| 8 | `dense_9` | Dense · gelu | `(B, 32)` | `(B, 32)` | 1,056 |
| 9 | `dropout_9` | Dropout 0.1 | `(B, 32)` | `(B, 32)` | 0 |
| 10 | `linear_trunk` | Dense · linear | `(B, 10)` | `(B, 1)` | 11 |
| 11 | `correction` | Dense · linear | `(B, 32)` | `(B, 1)` | 33 |
| 12 | `residual_mix` | ScaledResidual | `(B, 1)` + `(B, 1)` | `(B, 1)` | 1 |
| 13 | `flow_rate_lpm` | Rescaling | `(B, 1)` | `(B, 1)` | 0 |
| | | | | **합계** | **1,455** |

10번 `linear_trunk`의 입력이 9번이 아니라 **5번 `multi_scale`**이라는 점이,
5번에서 갈래가 나뉜다는 증거입니다.

## 평가 방식

세션이 바뀌면 압력-유량 관계가 이동하므로, 세션 단위로 채점합니다.

| 갈래 | 데이터 | 용도 |
|---|---|---|
| 학습 | `data/train_data` 앞 85% | 가중치 학습 |
| 조기 종료 | `data/train_data` 뒤 15% | 언제 멈출지만 결정 |
| 평가 | `data/test_data`의 **6개 세션** | 학습에 전혀 관여하지 않음 |

**조기 종료를 로그 세션이 아니라 학습 데이터 안에서 끊습니다.** 세션으로 끊으면
그 세션에 맞춰진 모델이 나옵니다 — 이전 구조가 검증 세션에서 MAPE 1.98%를 내고
처음 보는 세션에서 9.49%로 무너진 것이 정확히 그 경로였습니다. 이렇게 두면
여섯 세션이 전부 정직한 평가 세트가 됩니다.

### 현재 성능 (5시드 앙상블)

| 세션 | MAPE | MAE | 편향 |
|---|---:|---:|---:|
| 2025-11-14 17:06 | 5.93% | 829.8 | −810.3 |
| 2025-11-17 14:28 | 3.10% | 306.3 | +306.3 |
| 2025-11-17 14:32 | 4.72% | 540.2 | +539.0 |
| 2025-11-17 14:35 | 5.79% | 707.7 | +707.7 |
| 2025-11-17 14:36 | 2.79% | 385.4 | +385.4 |
| 2025-11-17 14:38 | 0.98% | 147.1 | +147.1 |
| **평균 / 최악** | **3.89% / 5.93%** | | |

> **세션별 R²는 보지 마십시오.** 한 세션 안에서는 유량 변화 폭이 작아(어떤
> 세션은 표준편차 200 LPM 미만) R²가 −16까지 내려갑니다. "그 세션의 평균을
> 찍는 것보다 나쁘다"는 뜻일 뿐, 예측이 망가졌다는 뜻이 아닙니다. 위 표에서
> **MAE ≈ |편향|** 인 것이 그 증거입니다 — 오차의 거의 전부가 세션별 영점
> 차이입니다. 세션 단위 평가에는 MAPE와 편향을 쓰십시오.

### 기존 구조와의 비교

교체 전 팽창 CNN(128,833 파라미터)과의 비교입니다. 다만 **프로토콜이 다릅니다** —
아래 두 줄은 11-17 세션 5개를 조기 종료에 쓰고 11-14 세션 하나로만 채점한
예전 방식의 숫자입니다.

| 구조 | 파라미터 | 검증 MAPE | 11-14 세션 MAPE |
|---|---:|---:|---:|
| 기존 팽창 CNN | 128,833 | 1.98% | 9.49% |
| 얇은 TCN + GAP | 18,945 | 4.02% | 8.16% |
| v2 | 1,455 | 3.36% | **5.93%** |

기존 구조의 검증 MAPE 1.98%가 가장 좋아 보이지만 처음 보는 세션에서 9.49%로
무너집니다. **기존 노트북이 보고하던 `R² 0.94`가 바로 이 낙관적인 쪽 숫자입니다.**

ONNX 파일 크기는 2,009 KB → 15 KB 입니다.

## 왜 이 구조인가 — 측정한 근거

전부 이 저장소의 로그 데이터에서 잰 값입니다.

### 1. 창의 역할은 '동특성'이 아니라 '잡음 제거'였다

입력만 바꿔가며 선형 회귀:

| 입력 | MAPE |
|---|---:|
| 마지막 1샘플 | 4.75% |
| 뒤쪽 5샘플 평균 | 4.52% |
| 20샘플 전체 평균 | 4.34% |

단순 평균만으로 좋아집니다. 그래서 평균을 팽창 합성곱이 스스로 발견하도록 두지
않고 `MultiScaleSmoothing`이 직접 만들어 넘깁니다.

### 2. 선형 모델이 이미 기존 심층망만큼 정확했다

20샘플 평균 압력에 대한 **3-파라미터 선형 회귀**가 기존 50만 파라미터 모델과
사실상 같은 검증 성능을 냈습니다. 용량이 정확도로 바뀌지 않고 학습 세션을
외우는 데 쓰이고 있었다는 뜻입니다.

그래서 선형 줄기를 주축으로 두고, 비선형은 `ScaledResidual`의 `alpha`가 자라는
만큼만 개입하게 했습니다. 학습 후 `alpha`를 찍어보면 비선형 항의 실제 기여를
숫자 하나로 확인할 수 있습니다.

### 3. 흡입압은 날것이 아니라 차압으로 넣어야 한다

세션별로 따로 선형 회귀를 맞추면 흡입압 계수는 **부호까지 뒤집힙니다**:

| 세션 | 흡입압 계수 | 토출압 계수 |
|---|---:|---:|
| 학습 | **−7,719** | 769 |
| 11-17 | **−244** | 825 |
| 11-14 | **+1,177** | 890 |

그래서 넣는 방식을 바꿔가며 6개 세션에서 각각 재봤습니다(5시드 앙상블, 조기
종료는 학습 데이터 뒤쪽 15%):

| 입력 구성 | 세션 평균 MAPE | 최악 |
|---|---:|---:|
| 토출압만 | 3.97% | 5.93% |
| 토출 + 흡입 (게이트 중립) | 4.55% | 6.82% |
| 토출 + 흡입 (게이트 억제) | 4.31% | 6.49% |
| 토출 + 흡입 (벌점 강화) | 4.30% | 6.43% |
| **토출 + 차압** | **3.90%** | 5.94% |
| 차압만 | 4.02% | 5.96% |
| 흡입압만 | 8.71% | 15.41% |

**날것으로 넣으면 6개 세션 전부에서 나빠집니다.** 특히 게이트를 중립(1.0)에서
공평하게 출발시킨 조건이 가장 나쁩니다 — 흡입압을 억제해서 얻은 결과가 아닙니다.

**차압으로 넣으면 손해가 없습니다.** 차압은 펌프가 실제로 만들어 내는 양정이고,
흡입측 영점이 세션마다 달라져도 토출압과 같이 움직이는 만큼 상쇄됩니다.

> 정직하게: 토출압만(3.97%)과 토출+차압(3.90%)의 차이 0.07%p는 **시드 편차
> (±0.2%p) 안이라 "더 좋다"고 주장할 수 없습니다.** 확실한 것은 두 가지입니다 —
> 차압으로 넣으면 잃는 게 없고, 날것으로 넣으면 6개 세션 전부에서 잃습니다.

날것 2채널을 쓰려면 `build_flow_model_v2(..., use_differential=False)` 입니다.

### 4. 절대 크기 정보를 버리지 않는다

이전 모델은 `Flatten` 직후 샘플 단위 정규화를 걸어, 마지막 단이 "패턴 모양"만
보게 되어 있었습니다. v2는 **학습 데이터 통계로 만든 고정 상수 스케일링**만
씁니다. 유량은 압력의 절대값에서 읽어야 하는 값이기 때문입니다.

타깃 평균/표준편차도 마지막 `Rescaling` 레이어에 접어 넣어, 학습은 표준화된
공간에서 하되 모델이 내보내는 값은 그대로 LPM입니다. 추론 쪽 후처리가 없습니다.

### 5. 회전수는 입력이 아니다

모델이 받는 것은 압력 두 채널뿐이므로, **입력이 곧 모델이 쓰는 것**입니다.
회전수는 `inference_modbus.py`에서 계속 읽지만 로깅과 `rpm <= 10` 정지 판정에만
쓰입니다.

> 참고: 이전 배포 모델(`models/model_20.onnx`)은 회전수를 **의도치 않게**
> 무시하고 있었습니다. 크기가 1인 축에 `LayerNormalization(axis=-1)`을 걸어
> 그 채널이 상수가 되어 있었기 때문입니다.

## 절제 실험

값을 하는 것과 하지 않는 것을 분리했습니다(단일 홀드아웃 세션 기준).

| 구성 | MAPE | 판정 |
|---|---:|---|
| v2 전체 | 5.69% | — |
| − 평활 제거 (flatten) | 5.66% | 차이 없음 (파라미터 38% 증가) |
| − 선형 줄기 제거 | 5.74% | 나빠짐 |
| − 비선형 전부 제거 (7개 파라미터) | 5.96% | 나빠짐 |
| 폭 64 | 5.67% | 이득 없음 |
| weight decay 1e-3 | 5.85% | 나빠짐 |
| 8시드 앙상블 | 5.66% | 이득 0.03%p |

센서 드리프트 증강(입력 압력에 랜덤 오프셋 추가)도 시도했습니다. 검증 MAPE는
크게 좋아지지만(3.99% → 2.58%) 홀드아웃은 나빠집니다(6.81%) — 검증 세션에만
맞춰지는 또 다른 형태의 과적합이라 채택하지 않았습니다.

손실 함수는 logcosh를 유지합니다. Huber(δ=200/500)가 0.06~0.08%p 앞서지만 시드
편차 안이고, MSE는 명확히 열위(6.46%)입니다.

## 데이터 처리에서 고친 점

* 학습 csv에는 헤더 행이 없는데 `pd.read_csv(...)`를 `header=None` 없이 부르면
  첫 샘플이 헤더로 먹혀 사라집니다 → `header=None` 지정.
* 운전 범위 밖 샘플을 지운 뒤 이어붙이면 시퀀스 창이 그 틈을 가로지릅니다
  → 연속 구간 단위로 나눈 뒤 구간 안에서만 창을 만듭니다(`split_valid_runs`).
* 회전수는 운전 구간을 고르는 데만 쓰이고 창에는 담기지 않습니다.

## 사용법

```bash
python train_flow_model.py                 # 학습 + 6개 세션 성능 + models/model_20_v2.keras 저장
python train_flow_model.py --no-save       # 숫자만 확인
python -m utils.converte_onnx              # models/model_20_v2.onnx 로 변환
```

`inference_modbus.py`는 `models/model_20_v2.onnx`를 읽고 입력 버퍼는
`(1, 20, 2)` = `[흡입압, 토출압]` 입니다.

## 남은 한계

* **세션 간 바이어스가 남은 오차의 대부분입니다.** 위 표에서 MAE와 |편향|이
  거의 같습니다. 모델을 키우기보다 **현장 1점 보정**(운전 중 유량계 값으로
  오프셋을 한 번 맞추는 것)이 효과가 클 가능성이 높습니다.
* **세션이 6개뿐입니다.** 그중 5개는 같은 날 연속 측정이라 사실상 독립적인
  조건은 2개에 가깝습니다. 조건이 다른 로그가 쌓이면 재검증이 필요합니다.
* **저유량 구간은 검증되지 않았습니다.** 연속 20스텝 조건을 만족하는 창 기준으로
  모든 세션이 약 7,000 LPM 이상만 담고 있습니다.
* **흡입측 조건이 거의 변하지 않았습니다.** 흡입 양정·호스 길이를 바꾼 로그가
  쌓이면 차압 형태가 여전히 최선인지 다시 확인해야 합니다.
