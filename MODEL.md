# 유량 추정 모델 (v2)

`utils.assemble_model.build_flow_model_v2`

## 한 줄 요약

토출 압력 1채널의 20스텝 창을 여러 길이로 평균 낸 뒤, **선형 줄기**로 유량을
예측하고 그 위에 **작은 비선형 보정**을 얹습니다. 파라미터는 1,289개입니다.

```
입력 (20, 3)  ← 로깅/추론 버퍼는 3채널 그대로
   │
   ├ ChannelSelect(1)             토출 압력만 통과. 회전수·흡입압은 구조적으로 차단
   ├ Rescaling(고정 상수)          학습 데이터 평균/표준편차. 샘플별 정규화 금지
   ├ ChannelGate                   채널별 학습 스칼라 + L2 벌점
   ├ MultiScaleSmoothing(1,3,5,10,20)   뒤쪽 1·3·5·10·20스텝 평균 → 5차원
   │
   ├──────────────── linear_trunk : Dense(1, linear)          ← 예측의 주축
   │
   └ Dense(32) → Dense(32) → correction : Dense(1, linear)    ← 보정
                    │
              ScaledResidual : trunk + alpha * correction  (alpha는 0에서 시작)
                    │
              Rescaling(target_std, target_mean) → 유량(LPM)
```

## 성능

세션이 다르면 압력-유량 관계가 이동하므로 세 갈래로 나눠 평가합니다.

| 갈래 | 데이터 | 용도 |
|---|---|---|
| 학습 | `data/train_data/*.csv` (3,477 창) | 가중치 학습 |
| 검증 | `data/test_data/data20251117-*.csv` (1,390 창) | 조기 종료·학습률 조정 → **낙관적** |
| 테스트 | `data/test_data/data20251114-170633.csv` (632 창) | 아무 데도 쓰지 않음 → **이 숫자를 믿는다** |

테스트 파일은 기존 노트북이 한 번도 쓰지 않던 세션입니다.

### 기존 구조 대비 (4시드 앙상블)

| | 파라미터 | val R² | val MAPE | **TEST R²** | **TEST MAE** | **TEST MAPE** |
|---|---:|---:|---:|---:|---:|---:|
| 기존 팽창 CNN | 128,833 | 0.981 | 1.98% | 0.502 | 1,306.5 | 9.49% |
| 얇은 TCN + GAP | 18,945 | 0.926 | 4.02% | 0.632 | 1,128.9 | 8.16% |
| **v2 (이 모델)** | **1,289** | 0.944 | 3.36% | **0.768** | **820.4** | **5.89%** |

기존 구조의 val 성적(MAPE 1.98%, R² 0.981)이 가장 좋아 보이지만, 처음 보는
세션에서는 9.49%로 무너집니다. 조기 종료를 검증 세션으로 걸면서 12.9만
파라미터가 그 세션에 맞춰진 결과입니다. **기존 노트북이 보고하던 "R² 0.94"는
이 낙관적인 쪽 숫자입니다.**

ONNX 파일 크기는 2,009 KB → 14 KB 입니다.

## 왜 이 구조인가 — 측정한 근거

전부 이 저장소의 로그 데이터에서 잰 값입니다.

### 1. 창의 역할은 '동특성'이 아니라 '잡음 제거'였다

압력 2채널 선형 회귀, 입력만 바꿔가며:

| 입력 | 검증 MAPE |
|---|---:|
| 마지막 1샘플 | 4.75% |
| 뒤쪽 5샘플 평균 | 4.52% |
| 20샘플 전체 평균 | 4.34% |

단순 평균만으로 좋아집니다. 20스텝 창이 담은 정보는 대부분 "같은 값을 여러 번
잰 것"입니다. 그래서 평균을 팽창 합성곱이 스스로 발견하도록 두지 않고
`MultiScaleSmoothing`이 직접 만들어 넘깁니다.

### 2. 선형 모델이 이미 기존 심층망만큼 정확했다

20샘플 평균 압력에 대한 **3-파라미터 선형 회귀**가 기존 50만 파라미터 모델과
사실상 같은 검증 성능을 냈습니다. 용량이 정확도로 바뀌지 않고 학습 세션을
외우는 데 쓰이고 있었다는 뜻입니다.

그래서 선형 줄기를 주축으로 두고, 비선형은 `ScaledResidual`의 `alpha`가 자라는
만큼만 개입하게 했습니다. 학습 후 `alpha`를 찍어보면 비선형 항의 실제 기여를
숫자 하나로 확인할 수 있습니다(현재 ±0.11~0.15).

### 3. 지배적 오차원은 '세션 간 이동'이고, 그 원인은 흡입압이었다

세션별로 따로 선형 회귀를 맞추면:

| 세션 | 흡입압 계수 | 토출압 계수 | 자체 R² |
|---|---:|---:|---:|
| 학습 (train_data) | **−7,719** | 769 | 0.939 |
| 검증 (11-17) | **−244** | 825 | 0.995 |
| 테스트 (11-14) | **+1,177** | 890 | 0.913 |

한 세션 안에서는 관계가 거의 완벽한 직선입니다(검증 R² 0.995). 그런데 **흡입압
계수는 세션이 바뀌면 부호까지 뒤집힙니다.** 토출압 계수는 769~890으로 안정적입니다.

흡입 채널을 누를수록 홀드아웃 성능이 단조롭게 좋아집니다:

| 흡입 채널 처리 | TEST MAPE |
|---|---:|
| 그대로 사용 | 6.56% |
| 게이트 L2 벌점 강화 (0.01 → 0.1) | 6.31% |
| **완전 제외** | **5.69%** |

> 이 판단의 근거(계수 부호 반전)는 학습·검증 세션만으로 이미 확인되므로,
> 테스트 세션은 결정을 **확인**한 것이지 결정을 **만든** 것이 아닙니다. 다만
> 구성 선택에 테스트 지표를 참고한 것은 사실이므로, 위 수치는 그만큼
> 낙관적일 수 있습니다.

**흡입압 센서를 코드에서 지우지는 않았습니다.** 흡입측 정보(호스 길이, 흡입
양정)는 물리적으로 의미가 있고, 지금 데이터에서 그 조건이 거의 변하지 않아
드러나지 않는 것일 수 있기 때문입니다. 되돌리는 방법은 인자 하나입니다:

```python
build_flow_model_v2(..., feature_channels=(0, 1), gate_init=(0.3, 1.0))
```

### 4. 절대 크기 정보를 버리지 않는다

이전 모델은 `Flatten` 직후 샘플 단위 정규화를 걸어, 마지막 단이 "패턴 모양"만
보게 되어 있었습니다. v2는 **학습 데이터 통계로 만든 고정 상수 스케일링**만
씁니다. 유량은 압력의 절대값에서 읽어야 하는 값이기 때문입니다.

타깃 평균/표준편차도 마지막 `Rescaling` 레이어에 접어 넣어, 학습은 표준화된
공간에서 하되 모델이 내보내는 값은 그대로 LPM입니다. 추론 쪽 후처리가 없습니다.

### 5. 회전수는 쓰지 않는다

`ChannelSelect(channels=(1,))`가 입력에서 회전수 채널을 잘라냅니다. 입력 shape는
`(20, 3)` 그대로라 로깅 포맷과 `inference_modbus.py`의 버퍼를 바꿀 필요가 없고,
동시에 회전수가 예측에 영향을 줄 수 없음이 구조로 보장됩니다. ONNX 모델에
직접 값을 넣어 확인했습니다(회전수·흡입압 변경 시 출력 변화량 정확히 0).

> 참고: 이전 배포 모델(`models/model_20.onnx`)은 회전수를 **의도치 않게**
> 무시하고 있었습니다. 크기가 1인 축에 `LayerNormalization(axis=-1)`을 걸어
> 그 채널이 상수가 되어 있었기 때문입니다. v2에서는 사고가 아니라 명시적인
> 설계 결정입니다.

## 절제 실험

값을 하는 것과 하지 않는 것을 분리했습니다. 모두 TEST MAPE 기준입니다.

**압력 2채널일 때**

| 구성 | TEST MAPE |
|---|---:|
| v2 전체 | 6.56% |
| − 평활 제거 (flatten) | 6.53% (차이 없음) |
| + 샘플별 정규화 복원 | 6.57% (차이 없음) |
| − 비선형 전부 제거 (13개 파라미터) | 6.57% (차이 없음) |
| − 선형 줄기 제거 | 6.84% (**나빠짐**) |
| − 흡입 채널 게이트 제거 | 6.96% (**나빠짐**) |

**토출 1채널일 때 (최종 구성)**

| 구성 | 파라미터 | TEST MAPE |
|---|---:|---:|
| **v2 전체** | **1,289** | **5.69%** |
| − 평활 제거 | 1,784 | 5.66% (차이 없음, 파라미터 38% 증가) |
| − 선형 줄기 제거 | 1,282 | 5.74% |
| − 비선형 전부 제거 (7개 파라미터) | 7 | 5.96% (**나빠짐**) |
| 폭 64 | 4,617 | 5.67% (이득 없음) |
| weight decay 1e-3 | 1,289 | 5.85% |
| 8시드 앙상블 | 1,289 | 5.66% (이득 0.03%p) |

흥미로운 점: 2채널일 때는 비선형 보정의 기여가 0이었는데, 흡입 채널의 잡음이
사라지자 값을 하기 시작합니다(5.96% → 5.69%).

센서 드리프트 증강(입력 압력에 랜덤 오프셋 추가)도 시도했습니다. 검증 MAPE는
크게 좋아지지만(3.99% → 2.58%) 테스트는 나빠집니다(6.81%) — 검증 세션에만
맞춰지는 또 다른 형태의 과적합이라 채택하지 않았습니다.

손실 함수는 logcosh를 유지합니다. Huber(δ=200/500)가 0.06~0.08%p 앞서지만
시드 편차(±0.1%p) 안이고, MSE는 명확히 열위(6.46%)입니다.

## 데이터 처리에서 고친 점

* 학습 csv에는 헤더 행이 없는데 `pd.read_csv(...)`를 `header=None` 없이 부르면
  첫 샘플이 헤더로 먹혀 사라집니다 → `header=None` 지정.
* 운전 범위 밖 샘플을 지운 뒤 이어붙이면 시퀀스 창이 그 틈을 가로지릅니다
  → 연속 구간 단위로 나눈 뒤 구간 안에서만 창을 만듭니다(`split_valid_runs`).

## 사용법

```bash
python train_flow_model.py                 # 학습 + 3갈래 성능 출력 + models/model_20_v2.keras 저장
python train_flow_model.py --no-save       # 숫자만 확인
python -m utils.converte_onnx              # models/model_20_v2.onnx 로 변환
```

`inference_modbus.py`는 `models/model_20_v2.onnx`를 읽습니다. 입력 버퍼 모양은
이전과 같습니다.

## 남은 한계

* **테스트 세션이 하나뿐입니다.** 홀드아웃 수치는 그 한 세션에 대한 값이라
  신뢰구간이 넓습니다. 세션이 더 쌓이면 leave-one-session-out 교차검증으로
  바꾸는 편이 낫습니다.
* **세션 간 바이어스는 남아 있습니다.** 테스트 세션의 예측 편향은 약 −806 LPM
  이고, 그 세션 자체 평균 오프셋을 제거하면 MAE가 926 → 583으로 줄어듭니다.
  즉 남은 오차의 상당 부분은 모델 구조가 아니라 세션별 영점 차이입니다.
  현장 1점 보정(운전 중 유량계 값으로 오프셋을 한 번 맞추는 것)이 구조를 더
  키우는 것보다 효과가 클 가능성이 높습니다.
* **저유량 구간은 검증되지 않았습니다.** 연속 20스텝 조건을 만족하는 창 기준으로
  세 갈래 모두 약 7,000 LPM 이상만 담고 있습니다.
